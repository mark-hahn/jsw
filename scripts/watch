#   -*-  grammar-ext: sh  -*-

function block_for_change {
  inotifywait -q src/* test/js-in.js
}
# count=0
# function build {
#   # count=$((count+1))
#   # echo "$count"
#   bash scripts/run
# }
bash scripts/run
while block_for_change; do
  bash scripts/run
done
