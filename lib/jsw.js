// Generated by CoffeeScript 1.9.3

/*
  jsw.coffee
  A translator for an alternate Javascript syntax that uses significant whitespace
 */

(function() {
  var Uglify, UglifyF, UglifyT, args, ast, chlklin, codeIn, codeOut, file, fs, i, jswMappings, len, log, meta, metaObj, metaStr, opts, ref, ref1, utils;

  log = require('debug')('jsw');

  fs = require('fs');

  utils = require('./utils');

  args = require('./args');

  Uglify = require('uglify-js2');

  UglifyT = require('uglify-js2-tojsw');

  UglifyF = require('uglify-js2-fromjsw');

  meta = require('./meta');

  chlklin = require('chalkline');

  ref = args.files;
  for (i = 0, len = ref.length; i < len; i++) {
    file = ref[i];
    chlklin.magenta();
    if (args.tojsw) {
      codeIn = fs.readFileSync(file + '.js', 'utf8');
      ast = UglifyT.parse(codeIn);
      utils.dumpAst(ast);
      fs.writeFileSync('test/ast.json', JSON.stringify(ast));
      opts = {
        beautify: true,
        indent_level: 2
      };
      if (args.map) {
        jswMappings = [];
        opts.node_map = {
          add: function(node_gen_map) {
            return jswMappings.push(node_gen_map);
          }
        };
      }
      codeOut = ast.print_to_string(opts);
      metaStr = (args.map ? meta.encode(codeIn, codeOut, jswMappings) : '');
      fs.writeFileSync(file + '.jsw', codeOut + metaStr);
    }
    if (args.beautifyjs) {
      codeIn = fs.readFileSync(file + '.js', 'utf8');
      ast = Uglify.parse(codeIn);
      utils.dumpAst(ast, 'test/b');
      fs.writeFileSync('test/b-ast.json', JSON.stringify(ast));
      codeOut = ast.print_to_string({
        beautify: true
      });
      fs.writeFileSync('test/b-out.js', codeOut);
    }
    if (args.fromjsw) {
      codeIn = fs.readFileSync(file + '.jsw', 'utf8');
      if (args.map) {
        ref1 = meta.decode(codeIn), codeIn = ref1[0], metaObj = ref1[1];
        if (!codeIn) {
          throw 'metadata in jsw file is missing or corrupted';
        }
      }
      ast = UglifyF.parse(codeIn);
      utils.dumpAst(ast);
      fs.writeFileSync('test/ast.json', JSON.stringify(ast));
      opts = {
        beautify: true,
        indent_level: 2
      };
      codeOut = ast.print_to_string(opts);
      fs.writeFileSync('test/meta.json', JSON.stringify(metaObj != null ? metaObj : {}));
      fs.writeFileSync(file + '.js', codeOut);
    }
    chlklin.blue();
  }

}).call(this);
