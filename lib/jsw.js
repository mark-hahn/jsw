// Generated by CoffeeScript 1.9.3

/*
  jsw.coffee
  A translator for an alternate Javascript syntax that uses significant whitespace
 */

(function() {
  var Uglify, UglifyFrom, UglifyT0, args, ast, chlklin, file, fileBase, fileNoExt, fs, i, jsCode, jswCode, jswMappings, len, log, meta, metaObj, metaStr, opts, ref, ref1, ref2, ref3, ref4, utils;

  log = require('debug')('jsw');

  fs = require('fs');

  utils = require('./utils');

  args = require('./args');

  UglifyT0 = require('../uglify/tojsw/node');

  UglifyFrom = require('../uglify/fromjsw/node');

  meta = require('./meta');

  chlklin = require('chalkline');

  ref = args.files;
  for (i = 0, len = ref.length; i < len; i++) {
    file = ref[i];
    chlklin.magenta();
    if (args.tojsw) {
      ref1 = utils.checkFileExt(file, '.js'), fileNoExt = ref1[0], fileBase = ref1[1];
      jsCode = fs.readFileSync(file, 'utf8');
      ast = UglifyT0.parse(jsCode);
      utils.dumpAst(ast, 'test/ast-dump.json');
      fs.writeFileSync('test/ast.json', JSON.stringify(ast));
      opts = {
        beautify: true,
        indent_level: 2
      };
      if (args.map) {
        jswMappings = [];
        opts.node_map = {
          add: function(node_gen_map) {
            return jswMappings.push(node_gen_map);
          }
        };
      }
      jswCode = ast.print_to_string(opts);
      metaStr = (args.map ? meta.encode(jsCode, jswCode, jswMappings) : '');
      fs.writeFileSync(fileNoExt + '.jsw', jswCode + metaStr);
    }
    if (args.beautifyjs) {
      Uglify = require('uglify-js2');
      ref2 = utils.checkFileExt(file, '.js'), fileNoExt = ref2[0], fileBase = ref2[1];
      jsCode = fs.readFileSync(file, 'utf8');
      ast = Uglify.parse(jsCode);
      utils.dumpAst(ast, 'test/ast-dump' + fileBase + '.json');
      fs.writeFileSync('test/b-ast.json', JSON.stringify(ast));
      jsCode = ast.print_to_string({
        beautify: true
      });
      fs.writeFileSync('test/b-out.js', jsCode);
    }
    if (args.fromjsw) {
      ref3 = utils.checkFileExt(file, '.jsw'), fileNoExt = ref3[0], fileBase = ref3[1];
      jswCode = fs.readFileSync(file, 'utf8');
      if (args.map) {
        ref4 = meta.decode(jswCode), jswCode = ref4[0], metaObj = ref4[1];
        metaObj.jswCode = jswCode;
        if (!jswCode) {
          throw 'jsw metadata is missing, corrupted, or unknown version';
        }
        fs.writeFileSync('test/meta.json', JSON.stringify(metaObj != null ? metaObj : {}));
      }
      ast = UglifyFrom.parse(jswCode);
      utils.dumpAst(ast, 'test/ast-dump.json');
      fs.writeFileSync('test/ast.json', JSON.stringify(ast));
      opts = {
        beautify: true,
        indent_level: 2
      };
      if (args.map) {
        opts.jsw_out_meta = metaObj;
      }
      jsCode = ast.print_to_string(opts);
      fs.writeFileSync(fileNoExt + '.js', jsCode);
    }
    chlklin.blue();
  }

}).call(this);
